# Generated by scripts/generate_info_modules.py -- regenerate instead of editing.
from __future__ import absolute_import, division, print_function

__metaclass__ = type

import sys
import types

import pytest

from ansible_collections.tencentcloud.cloud.plugins.modules import bi_auth_api_key_info


class FakeRequest:
    pass


class FakeModels:
    DescribeAuthApiKeyListRequest = FakeRequest


def test_build_request_sets_pagination():
    request = bi_auth_api_key_info.build_request(FakeModels, 200, 100)
    assert request.PageNo == 3
    assert request.PageSize == 100


class FakeItem:
    def __init__(self, marker):
        self.marker = marker

    def _serialize(self, allow_none=True):
        return {"Marker": self.marker}


class FakeResponse:
    def __init__(self, items, total_count):
        self.Data = types.SimpleNamespace(List=items, Total=total_count)


class FakeClient:
    def __init__(self, pages):
        self._pages = list(pages)
        self.requests = []

    def DescribeAuthApiKeyList(self, request):
        self.requests.append(request)
        return self._pages.pop(0)


class ModuleExit(Exception):
    pass


class FakeModule:
    def __init__(self, params):
        self.params = params
        self.exit_payload = None

    def exit_json(self, **kwargs):
        self.exit_payload = kwargs
        raise ModuleExit()

    def fail_json(self, **kwargs):
        raise AssertionError("fail_json called: %r" % (kwargs,))


def _run(monkeypatch, client, **params):
    service = types.ModuleType("tencentcloud.bi.v20220105")
    service.models = FakeModels
    service.bi_client = types.SimpleNamespace(BiClient=lambda *args: client)
    monkeypatch.setitem(sys.modules, "tencentcloud", types.ModuleType("tencentcloud"))
    monkeypatch.setitem(sys.modules, "tencentcloud.bi",
                        types.ModuleType("tencentcloud.bi"))
    monkeypatch.setitem(sys.modules, "tencentcloud.bi.v20220105", service)
    fake = FakeModule(params)
    monkeypatch.setattr(bi_auth_api_key_info, "AnsibleModule", lambda **kwargs: fake)
    monkeypatch.setattr(bi_auth_api_key_info, "create_credential", lambda module: object())
    monkeypatch.setattr(bi_auth_api_key_info, "create_client_profile", lambda module, endpoint: object())
    with pytest.raises(ModuleExit):
        bi_auth_api_key_info.run_module()
    return fake


def test_run_module_paginates_until_total_count(monkeypatch):
    client = FakeClient([
        FakeResponse([FakeItem("a"), FakeItem("b")], 3),
        FakeResponse([FakeItem("c")], 3),
    ])
    fake = _run(monkeypatch, client, region="ap-guangzhou",
                page_size=2)
    payload = fake.exit_payload
    assert payload["changed"] is False
    assert [item["Marker"] for item in payload["auth_api_keys"]] == ["a", "b", "c"]
    assert payload["total_count"] == 3
    assert [request.PageNo for request in client.requests] == [1, 2]
