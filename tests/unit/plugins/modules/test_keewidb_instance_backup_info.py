# Generated by scripts/generate_info_modules.py -- regenerate instead of editing.
from __future__ import absolute_import, division, print_function

__metaclass__ = type

import sys
import types

import pytest

from ansible_collections.tencentcloud.cloud.plugins.modules import keewidb_instance_backup_info


class FakeRequest:
    pass


class FakeModels:
    DescribeInstanceBackupsRequest = FakeRequest


def test_build_request_sets_pagination():
    request = keewidb_instance_backup_info.build_request(FakeModels, "sample", 200, 100)
    assert request.Offset == 200
    assert request.Limit == 100


class FakeItem:
    def __init__(self, marker):
        self.marker = marker

    def _serialize(self, allow_none=True):
        return {"Marker": self.marker}


class FakeResponse:
    def __init__(self, items, total_count):
        self.BackupRecord = items
        self.TotalCount = total_count


class FakeClient:
    def __init__(self, pages):
        self._pages = list(pages)
        self.requests = []

    def DescribeInstanceBackups(self, request):
        self.requests.append(request)
        return self._pages.pop(0)


class ModuleExit(Exception):
    pass


class FakeModule:
    def __init__(self, params):
        self.params = params
        self.exit_payload = None

    def exit_json(self, **kwargs):
        self.exit_payload = kwargs
        raise ModuleExit()

    def fail_json(self, **kwargs):
        raise AssertionError("fail_json called: %r" % (kwargs,))


def _run(monkeypatch, client, **params):
    service = types.ModuleType("tencentcloud.keewidb.v20220308")
    service.models = FakeModels
    service.keewidb_client = types.SimpleNamespace(KeewidbClient=lambda *args: client)
    monkeypatch.setitem(sys.modules, "tencentcloud", types.ModuleType("tencentcloud"))
    monkeypatch.setitem(sys.modules, "tencentcloud.keewidb",
                        types.ModuleType("tencentcloud.keewidb"))
    monkeypatch.setitem(sys.modules, "tencentcloud.keewidb.v20220308", service)
    fake = FakeModule(params)
    monkeypatch.setattr(keewidb_instance_backup_info, "AnsibleModule", lambda **kwargs: fake)
    monkeypatch.setattr(keewidb_instance_backup_info, "create_credential", lambda module: object())
    monkeypatch.setattr(keewidb_instance_backup_info, "create_client_profile", lambda module, endpoint: object())
    with pytest.raises(ModuleExit):
        keewidb_instance_backup_info.run_module()
    return fake


def test_run_module_paginates_until_total_count(monkeypatch):
    client = FakeClient([
        FakeResponse([FakeItem("a"), FakeItem("b")], 3),
        FakeResponse([FakeItem("c")], 3),
    ])
    fake = _run(monkeypatch, client, region="ap-guangzhou",
                instance_id="sample", page_size=2)
    payload = fake.exit_payload
    assert payload["changed"] is False
    assert [item["Marker"] for item in payload["instance_backups"]] == ["a", "b", "c"]
    assert payload["total_count"] == 3
    assert [request.Offset for request in client.requests] == [0, 2]
