# Generated by scripts/generate_info_modules.py -- regenerate instead of editing.
from __future__ import absolute_import, division, print_function

__metaclass__ = type

import sys
import types

import pytest

from ansible_collections.susunola.tencentcloud.plugins.modules import fmu_model_info


class FakeRequest:
    pass


class FakeModels:
    GetModelListRequest = FakeRequest


def test_build_request_sets_pagination():
    request = fmu_model_info.build_request(FakeModels, 200, 100)
    assert request.Offset == 200
    assert request.Limit == 100


class FakeItem:
    def __init__(self, marker):
        self.marker = marker

    def _serialize(self, allow_none=True):
        return {"Marker": self.marker}


class FakeResponse:
    def __init__(self, items, total_count):
        self.ModelInfos = items


class FakeClient:
    def __init__(self, pages):
        self._pages = list(pages)
        self.requests = []

    def GetModelList(self, request):
        self.requests.append(request)
        return self._pages.pop(0)


class ModuleExit(Exception):
    pass


class FakeModule:
    def __init__(self, params):
        self.params = params
        self.exit_payload = None

    def exit_json(self, **kwargs):
        self.exit_payload = kwargs
        raise ModuleExit()

    def fail_json(self, **kwargs):
        raise AssertionError("fail_json called: %r" % (kwargs,))


def _run(monkeypatch, client, **params):
    service = types.ModuleType("tencentcloud.fmu.v20191213")
    service.models = FakeModels
    service.fmu_client = types.SimpleNamespace(FmuClient=lambda *args: client)
    monkeypatch.setitem(sys.modules, "tencentcloud", types.ModuleType("tencentcloud"))
    monkeypatch.setitem(sys.modules, "tencentcloud.fmu",
                        types.ModuleType("tencentcloud.fmu"))
    monkeypatch.setitem(sys.modules, "tencentcloud.fmu.v20191213", service)
    fake = FakeModule(params)
    monkeypatch.setattr(fmu_model_info, "AnsibleModule", lambda **kwargs: fake)
    monkeypatch.setattr(fmu_model_info, "create_credential", lambda module: object())
    monkeypatch.setattr(fmu_model_info, "create_client_profile", lambda module, endpoint: object())
    with pytest.raises(ModuleExit):
        fmu_model_info.run_module()
    return fake


def test_run_module_paginates_until_short_page(monkeypatch):
    client = FakeClient([
        FakeResponse([FakeItem("a"), FakeItem("b")], None),
        FakeResponse([FakeItem("c")], None),
    ])
    fake = _run(monkeypatch, client, region="ap-guangzhou",
                page_size=2)
    payload = fake.exit_payload
    assert payload["changed"] is False
    assert [item["Marker"] for item in payload["models"]] == ["a", "b", "c"]
    assert payload["total_count"] == 3
    assert [request.Offset for request in client.requests] == [0, 2]
