# Generated by scripts/generate_info_modules.py -- regenerate instead of editing.
from __future__ import absolute_import, division, print_function

__metaclass__ = type

import sys
import types

import pytest

from ansible_collections.tencentcloud.cloud.plugins.modules import config_aggregate_compliance_pack_info


class FakeRequest:
    pass


class FakeModels:
    ListAggregateCompliancePacksRequest = FakeRequest


def test_build_request_sets_pagination():
    request = config_aggregate_compliance_pack_info.build_request(FakeModels, 200, 100)
    assert request.Offset == 200
    assert request.Limit == 100


class FakeItem:
    def __init__(self, marker):
        self.marker = marker

    def _serialize(self, allow_none=True):
        return {"Marker": self.marker}


class FakeResponse:
    def __init__(self, items, total_count):
        self.Items = items
        self.Total = total_count


class FakeClient:
    def __init__(self, pages):
        self._pages = list(pages)
        self.requests = []

    def ListAggregateCompliancePacks(self, request):
        self.requests.append(request)
        return self._pages.pop(0)


class ModuleExit(Exception):
    pass


class FakeModule:
    def __init__(self, params):
        self.params = params
        self.exit_payload = None

    def exit_json(self, **kwargs):
        self.exit_payload = kwargs
        raise ModuleExit()

    def fail_json(self, **kwargs):
        raise AssertionError("fail_json called: %r" % (kwargs,))


def _run(monkeypatch, client, **params):
    service = types.ModuleType("tencentcloud.config.v20220802")
    service.models = FakeModels
    service.config_client = types.SimpleNamespace(ConfigClient=lambda *args: client)
    monkeypatch.setitem(sys.modules, "tencentcloud", types.ModuleType("tencentcloud"))
    monkeypatch.setitem(sys.modules, "tencentcloud.config",
                        types.ModuleType("tencentcloud.config"))
    monkeypatch.setitem(sys.modules, "tencentcloud.config.v20220802", service)
    fake = FakeModule(params)
    monkeypatch.setattr(config_aggregate_compliance_pack_info, "AnsibleModule", lambda **kwargs: fake)
    monkeypatch.setattr(config_aggregate_compliance_pack_info, "create_credential", lambda module: object())
    monkeypatch.setattr(config_aggregate_compliance_pack_info, "create_client_profile", lambda module, endpoint: object())
    with pytest.raises(ModuleExit):
        config_aggregate_compliance_pack_info.run_module()
    return fake


def test_run_module_paginates_until_total_count(monkeypatch):
    client = FakeClient([
        FakeResponse([FakeItem("a"), FakeItem("b")], 3),
        FakeResponse([FakeItem("c")], 3),
    ])
    fake = _run(monkeypatch, client, region="ap-guangzhou",
                page_size=2)
    payload = fake.exit_payload
    assert payload["changed"] is False
    assert [item["Marker"] for item in payload["aggregate_compliance_packs"]] == ["a", "b", "c"]
    assert payload["total_count"] == 3
    assert [request.Offset for request in client.requests] == [0, 2]
