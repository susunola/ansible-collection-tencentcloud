# Generated by scripts/generate_info_modules.py -- regenerate instead of editing.
from __future__ import absolute_import, division, print_function

__metaclass__ = type

import sys
import types

import pytest

from ansible_collections.susunola.tencentcloud.plugins.modules import cdwdoris_cluster_configs_history_info


class FakeRequest:
    pass


class FakeModels:
    DescribeClusterConfigsHistoryRequest = FakeRequest


def test_build_request_sets_pagination():
    request = cdwdoris_cluster_configs_history_info.build_request(FakeModels, "sample", "sample", "sample", None, 200, 100)
    assert request.Offset == 200
    assert request.Limit == 100


def test_build_request_maps_ids():
    request = cdwdoris_cluster_configs_history_info.build_request(FakeModels, "sample", "sample", "sample", ["x-1"], 0, 100)
    assert request.ComputeGroupIds == ["x-1"]


class FakeItem:
    def __init__(self, marker):
        self.marker = marker

    def _serialize(self, allow_none=True):
        return {"Marker": self.marker}


class FakeResponse:
    def __init__(self, items, total_count):
        self.ClusterConfHistory = items
        self.TotalCount = total_count
        self.RequestId = "req-page"


class FakeClient:
    def __init__(self, pages):
        self._pages = list(pages)
        self.requests = []

    def DescribeClusterConfigsHistory(self, request):
        self.requests.append(request)
        return self._pages.pop(0)


class ModuleExit(Exception):
    pass


class FakeModule:
    def __init__(self, params):
        self.params = params
        self.exit_payload = None

    def exit_json(self, **kwargs):
        self.exit_payload = kwargs
        raise ModuleExit()

    def fail_json(self, **kwargs):
        raise AssertionError("fail_json called: %r" % (kwargs,))


def _run(monkeypatch, client, **params):
    service = types.ModuleType("tencentcloud.cdwdoris.v20211228")
    service.models = FakeModels
    service.cdwdoris_client = types.SimpleNamespace(CdwdorisClient=lambda *args: client)
    monkeypatch.setitem(sys.modules, "tencentcloud", types.ModuleType("tencentcloud"))
    monkeypatch.setitem(sys.modules, "tencentcloud.cdwdoris",
                        types.ModuleType("tencentcloud.cdwdoris"))
    monkeypatch.setitem(sys.modules, "tencentcloud.cdwdoris.v20211228", service)
    fake = FakeModule(params)
    monkeypatch.setattr(cdwdoris_cluster_configs_history_info, "AnsibleModule", lambda **kwargs: fake)
    monkeypatch.setattr(cdwdoris_cluster_configs_history_info, "create_credential", lambda module: object())
    monkeypatch.setattr(cdwdoris_cluster_configs_history_info, "create_client_profile", lambda module, endpoint: object())
    with pytest.raises(ModuleExit):
        cdwdoris_cluster_configs_history_info.run_module()
    return fake


def test_run_module_paginates_until_total_count(monkeypatch):
    client = FakeClient([
        FakeResponse([FakeItem("a"), FakeItem("b")], 3),
        FakeResponse([FakeItem("c")], 3),
    ])
    fake = _run(monkeypatch, client, region="ap-guangzhou",
                instance_id="sample", start_time="sample", end_time="sample", cluster_configs_history_ids=None, page_size=2)
    payload = fake.exit_payload
    assert payload["changed"] is False
    assert [item["Marker"] for item in payload["cluster_configs_histories"]] == ["a", "b", "c"]
    assert payload["total_count"] == 3
    assert payload["request_id"] == "req-page"
    assert [request.Offset for request in client.requests] == [0, 2]
