# Generated by antsibull-docs / Sphinx. Only rst/index.rst, the hand-written
# pages in extra_rst/ and the built HTML site (build/html) are tracked; the
# doctree cache and the generated RST under rst/collections are regenerated on
# every build and stay ignored.
/build/doctrees
/rst/collections

# build.sh copies extra_rst/*.rst into rst/ before the Sphinx run, so every
# top-level page except the hand-maintained index is a build artifact.
/rst/*.rst
!/rst/index.rst
