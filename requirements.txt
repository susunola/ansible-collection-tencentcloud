# The _info module specs in scripts/info_specs_auto.py are regenerated
# against a specific SDK release; scripts/check_sdk_drift.py compares CI's
# installed tencentcloud-sdk-python to the GENERATED_SDK_VERSION stamp and
# fails on drift. SDK packages are therefore pinned exactly so CI installs
# the same versions the specs and contract tests were produced against
# (the sentinel checks the main package; the others are pinned in lockstep
# for reproducible builds).
# After upgrading any constraint, run scripts/discover_info_specs.py and
# scripts/generate_info_modules.py and commit the regeneration together
# with the bump.
tencentcloud-sdk-python==3.1.113
tencentcloud-sdk-python-tag==3.1.109
# The committed specs reference models from these per-product subpackages
# (the main package bundles most products but not these); the hidden
# required-param audit and contract tests need them importable.
tencentcloud-sdk-python-alb==3.1.155
tencentcloud-sdk-python-bdrc==3.1.165
tencentcloud-sdk-python-cloudrc==3.1.162
tencentcloud-sdk-python-cngw==3.1.165
tencentcloud-sdk-python-fwm==3.1.166
tencentcloud-sdk-python-portal==3.1.162
# cds/dbdc ARE bundled by the main package but at older versions whose
# models lack request classes the committed specs reference (e.g.
# cds DescribeAssetsListRequest, dbdc DescribeDBCustomClustersRequest);
# pin the subpackages so CI imports the same (newer) models as local.
tencentcloud-sdk-python-cds==3.1.157
tencentcloud-sdk-python-dbdc==3.1.163
# Same story for ga2 (DescribeAccelerateAreasRequest.Filters) and tokenhub
# (DescribeModelListRequest Offset/Limit/ModelIds): the main 3.1.113 bundle
# carries older models, the subpackages declare the fields the specs use.
tencentcloud-sdk-python-ga2==3.1.149
tencentcloud-sdk-python-tokenhub==3.1.161
# Write-module SDKs (batch 8): cvm_chc / mongodb_instance / gaap_proxy /
# cdn_domain / tcr_instance. The main 3.1.113 bundle does NOT ship these
# services, so their contract tests would silently skip in CI (the module
# code is imported but never exercised, dragging the coverage gate down).
# cvm is pinned newer on purpose: ChcHost.NetworkMode (needed by the
# cvm_chc network_mode drift check) only exists from 3.1.158.
tencentcloud-sdk-python-cvm==3.1.158
tencentcloud-sdk-python-mongodb==3.1.160
tencentcloud-sdk-python-gaap==3.1.33
tencentcloud-sdk-python-cdn==3.1.152
tencentcloud-sdk-python-tcr==3.1.166
# Only needed for the cos_* modules (qcloud_cos SDK; COS is not API 3.0).
cos-python-sdk-v5>=1.9.0
