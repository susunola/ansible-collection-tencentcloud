## Summary

<!-- What does this PR change and why? Link any related issues. -->

Fixes #

## Changes

<!--
- New module `foo` for managing ...
- Fix idempotency of `bar` when ...
-->

## Checklist

- [ ] `ansible-test sanity` passes locally for the changed files.
- [ ] `ansible-test units` passes locally (and new/changed code has unit
      tests under `tests/unit/`).
- [ ] A changelog fragment has been added under `changelogs/fragments/`
      (see
      [changelogs for collections](https://docs.ansible.com/ansible/latest/community/collection_development_process.html#creating-a-changelog-fragment));
      trivial changes use the `trivial` section.
- [ ] New modules/plugins include complete `DOCUMENTATION`, `EXAMPLES`,
      and `RETURN` blocks and set `version_added`.
- [ ] Documentation and comments are in English and credentials are never
      logged or committed.
