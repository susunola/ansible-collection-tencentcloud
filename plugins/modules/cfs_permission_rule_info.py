#!/usr/bin/python
# -*- coding: utf-8 -*-
# Generated by scripts/generate_info_modules.py -- regenerate instead of editing.
# Copyright: (c) 2026, Tencent Cloud Ansible Collection Contributors
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
from __future__ import absolute_import, division, print_function

__metaclass__ = type

DOCUMENTATION = r'''
---
module: cfs_permission_rule_info
short_description: Gather information about Tencent Cloud CFS permission group rules
version_added: "1.1.0"
description: Returns the rules of a CFS permission group.
options:
  pgroup_id:
    description: ID of the CFS permission group whose rules are returned.
    type: str
    required: true
extends_documentation_fragment: susunola.tencentcloud.tencentcloud
author: Tencent Cloud Ansible Collection Contributors (@susunola)
'''

EXAMPLES = r'''
- name: List the rules of a CFS permission group
  susunola.tencentcloud.cfs_permission_rule_info:
    region: ap-guangzhou
    pgroup_id: "pgroup-xxxxxxxx"

'''

RETURN = r'''
rules:
  description: Rules of the permission group.
  returned: always
  type: list
  elements: dict
total_count:
  description: Number of rules returned (the API reports no pageable total).
  returned: always
  type: int
request_id:
  description: Request ID of the last API call, for cross-referencing cloud audit logs.
  returned: always
  type: str
'''

from ansible.module_utils.basic import AnsibleModule
from ansible_collections.susunola.tencentcloud.plugins.module_utils.tencentcloud import (
    create_client_profile, create_credential, sdk_call, serialize_sdk_object,
    tencentcloud_argument_spec,
)


def build_request(models, pgroup_id, offset, limit):
    # DescribeCfsRules returns the full list in one call and is not
    # paginated; offset and limit are accepted for signature uniformity.
    request = models.DescribeCfsRulesRequest()
    request.PGroupId = pgroup_id
    return request


def run_module():
    argument_spec = tencentcloud_argument_spec()
    argument_spec.update({
        "pgroup_id": {"type": "str", "required": True},
    })
    module = AnsibleModule(
        argument_spec=argument_spec,
        supports_check_mode=True,
    )
    try:
        from tencentcloud.cfs.v20190719 import models, cfs_client
    except ImportError:
        module.fail_json(msg="The tencentcloud-sdk-python-cfs package is required.")

    client = cfs_client.CfsClient(
        create_credential(module), module.params["region"],
        create_client_profile(module, "cfs.tencentcloudapi.com"),
    )
    request = build_request(models, module.params["pgroup_id"], 0, 0)
    response = sdk_call(module, client.DescribeCfsRules, request)
    items = response.RuleList or []
    rules = [serialize_sdk_object(item) for item in items]
    module.exit_json(changed=False, rules=rules,
                     total_count=len(rules), request_id=response.RequestId)


def main():
    run_module()


if __name__ == "__main__":
    main()
