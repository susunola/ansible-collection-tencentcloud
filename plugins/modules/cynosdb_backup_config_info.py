#!/usr/bin/python
# -*- coding: utf-8 -*-
# Generated by scripts/generate_info_modules.py -- regenerate instead of editing.
# Copyright: (c) 2026, Tencent Cloud Ansible Collection Contributors
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
from __future__ import absolute_import, division, print_function

__metaclass__ = type

DOCUMENTATION = r'''
---
module: cynosdb_backup_config_info
short_description: Gather information about a Tencent Cloud CynosDB backup configuration
version_added: "1.2.0"
description: Returns the backup configuration of a CynosDB cluster.
options:
  cluster_id:
    description: ID of the CynosDB cluster whose backup configuration is returned.
    type: str
    required: true
extends_documentation_fragment: susunola.tencentcloud.tencentcloud
author: Tencent Cloud Ansible Collection Contributors (@susunola)
'''

EXAMPLES = r'''
- name: Show the backup configuration of a CynosDB cluster
  susunola.tencentcloud.cynosdb_backup_config_info:
    region: ap-guangzhou
    cluster_id: "cynosdbmysql-xxxxxxxx"

'''

RETURN = r'''
backup_config:
  description: The backup configuration of the cluster.
  returned: always
  type: dict
request_id:
  description: Request ID of the API call, for cross-referencing cloud audit logs.
  returned: always
  type: str
'''

from ansible.module_utils.basic import AnsibleModule
from ansible_collections.susunola.tencentcloud.plugins.module_utils.tencentcloud import (
    create_client_profile, create_credential, sdk_call, serialize_sdk_object,
    tencentcloud_argument_spec,
)


def build_request(models, cluster_id, offset, limit):
    # DescribeBackupConfig takes no request arguments and is not
    # paginated; offset and limit are accepted for signature uniformity.
    request = models.DescribeBackupConfigRequest()
    request.ClusterId = cluster_id
    return request


def run_module():
    argument_spec = tencentcloud_argument_spec()
    argument_spec.update({
        "cluster_id": {"type": "str", "required": True},
    })
    module = AnsibleModule(
        argument_spec=argument_spec,
        supports_check_mode=True,
    )
    try:
        from tencentcloud.cynosdb.v20190107 import models, cynosdb_client
    except ImportError:
        module.fail_json(msg="The tencentcloud-sdk-python-cynosdb package is required.")

    client = cynosdb_client.CynosdbClient(
        create_credential(module), module.params["region"],
        create_client_profile(module, "cynosdb.tencentcloudapi.com"),
    )
    request = build_request(models, module.params["cluster_id"], 0, 0)
    response = sdk_call(module, client.DescribeBackupConfig, request)
    backup_config = serialize_sdk_object(response)
    backup_config.pop("RequestId", None)
    module.exit_json(changed=False, backup_config=backup_config,
                     request_id=response.RequestId)


def main():
    run_module()


if __name__ == "__main__":
    main()
