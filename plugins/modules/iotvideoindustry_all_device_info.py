#!/usr/bin/python
# -*- coding: utf-8 -*-
# Generated by scripts/generate_info_modules.py -- regenerate instead of editing.
# Copyright: (c) 2026, Tencent Cloud Ansible Collection Contributors
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
from __future__ import absolute_import, division, print_function

__metaclass__ = type

DOCUMENTATION = r'''
---
module: iotvideoindustry_all_device_info
short_description: Gather information about Tencent Cloud IOTVIDEOINDUSTRY all devices
version_added: "0.8.0"
description: Returns IOTVIDEOINDUSTRY all devices visible in a Tencent Cloud region.
options:
  all_device_ids:
    description: All device IDs to return.
    type: list
    elements: str
  page_size:
    description: Number of results requested per API call.
    type: int
    default: 100
extends_documentation_fragment: susunola.tencentcloud.tencentcloud
author: Tencent Cloud Ansible Collection Contributors (@susunola)
'''

EXAMPLES = r'''
- name: List all all devices
  susunola.tencentcloud.iotvideoindustry_all_device_info:
    region: ap-guangzhou

- name: Find all devices by ID
  susunola.tencentcloud.iotvideoindustry_all_device_info:
    region: ap-guangzhou
    all_device_ids: [x-xxxxxxxx]
'''

RETURN = r'''
all_devices:
  description: Matching IOTVIDEOINDUSTRY all devices.
  returned: always
  type: list
  elements: dict
total_count:
  description: Number of all devices reported by the API.
  returned: always
  type: int
'''

from ansible.module_utils.basic import AnsibleModule
from ansible_collections.susunola.tencentcloud.plugins.module_utils.paging import Paginator
from ansible_collections.susunola.tencentcloud.plugins.module_utils.tencentcloud import (
    create_client_profile, create_credential, sdk_call, serialize_sdk_object,
    tencentcloud_argument_spec,
)


def build_request(models, all_device_ids, offset, limit):
    request = models.DescribeAllDeviceListRequest()
    request.Offset = offset
    request.Limit = limit
    if all_device_ids:
        request.DeviceIds = all_device_ids
    return request


def run_module():
    argument_spec = tencentcloud_argument_spec()
    argument_spec.update({
        "all_device_ids": {"type": "list", "elements": "str"},
        "page_size": {"type": "int", "default": 100},
    })
    module = AnsibleModule(
        argument_spec=argument_spec,
        supports_check_mode=True,
    )
    try:
        from tencentcloud.iotvideoindustry.v20201201 import models, iotvideoindustry_client
    except ImportError:
        module.fail_json(msg="The tencentcloud-sdk-python-iotvideoindustry package is required.")

    client = iotvideoindustry_client.IotvideoindustryClient(
        create_credential(module), module.params["region"],
        create_client_profile(module, "iotvideoindustry.tencentcloudapi.com"),
    )
    paginator = Paginator(
        module.params["page_size"],
        lambda offset, limit: build_request(models, module.params["all_device_ids"], offset, limit),
        lambda request: sdk_call(module, client.DescribeAllDeviceList, request),
        lambda response: response.Devices,
        lambda response: response.TotalCount,
    )
    item_set, total_count = paginator.fetch_all()
    all_devices = [serialize_sdk_object(item) for item in item_set]
    module.exit_json(changed=False, all_devices=all_devices, total_count=total_count)


def main():
    run_module()


if __name__ == "__main__":
    main()
