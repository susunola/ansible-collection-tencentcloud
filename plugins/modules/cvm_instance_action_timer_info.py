#!/usr/bin/python
# -*- coding: utf-8 -*-
# Generated by scripts/generate_info_modules.py -- regenerate instead of editing.
# Copyright: (c) 2026, Tencent Cloud Ansible Collection Contributors
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
from __future__ import absolute_import, division, print_function

__metaclass__ = type

DOCUMENTATION = r'''
---
module: cvm_instance_action_timer_info
short_description: Gather information about Tencent Cloud CVM instance action timers
version_added: "0.4.0"
description: Returns the scheduled action timers of CVM instances visible in a Tencent Cloud region.
options:
  instance_ids:
    description: Return only the action timers of these CVM instances.
    type: list
    elements: str
  action_timer_ids:
    description: Return only these action-timer IDs.
    type: list
    elements: str
extends_documentation_fragment: susunola.tencentcloud.tencentcloud
author: Tencent Cloud Ansible Collection Contributors (@susunola)
'''

EXAMPLES = r'''
- name: List all instance action timers
  susunola.tencentcloud.cvm_instance_action_timer_info:
    region: ap-guangzhou

- name: Find the action timers of one instance
  susunola.tencentcloud.cvm_instance_action_timer_info:
    region: ap-guangzhou
    instance_ids: [ins-xxxxxxxx]
'''

RETURN = r'''
action_timers:
  description: Matching action timers.
  returned: always
  type: list
  elements: dict
total_count:
  description: Number of action timers returned (the API reports no pageable total).
  returned: always
  type: int
request_id:
  description: Request ID of the last API call, for cross-referencing cloud audit logs.
  returned: always
  type: str
'''

from ansible.module_utils.basic import AnsibleModule
from ansible_collections.susunola.tencentcloud.plugins.module_utils.tencentcloud import (
    create_client_profile, create_credential, sdk_call, serialize_sdk_object,
    tencentcloud_argument_spec,
)


def build_request(models, instance_ids, action_timer_ids, offset, limit):
    # DescribeInstancesActionTimer returns the full list in one call and is not
    # paginated; offset and limit are accepted for signature uniformity.
    request = models.DescribeInstancesActionTimerRequest()
    if instance_ids is not None:
        request.InstanceIds = instance_ids
    if action_timer_ids is not None:
        request.ActionTimerIds = action_timer_ids
    return request


def run_module():
    argument_spec = tencentcloud_argument_spec()
    argument_spec.update({
        "instance_ids": {"type": "list", "elements": "str"},
        "action_timer_ids": {"type": "list", "elements": "str"},
    })
    module = AnsibleModule(
        argument_spec=argument_spec,
        supports_check_mode=True,
    )
    try:
        from tencentcloud.cvm.v20170312 import models, cvm_client
    except ImportError:
        module.fail_json(msg="The tencentcloud-sdk-python-cvm package is required.")

    client = cvm_client.CvmClient(
        create_credential(module), module.params["region"],
        create_client_profile(module, "cvm.tencentcloudapi.com"),
    )
    request = build_request(models, module.params["instance_ids"], module.params["action_timer_ids"], 0, 0)
    response = sdk_call(module, client.DescribeInstancesActionTimer, request)
    items = response.ActionTimers or []
    action_timers = [serialize_sdk_object(item) for item in items]
    module.exit_json(changed=False, action_timers=action_timers,
                     total_count=len(action_timers), request_id=response.RequestId)


def main():
    run_module()


if __name__ == "__main__":
    main()
