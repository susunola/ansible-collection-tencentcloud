#!/usr/bin/python
# -*- coding: utf-8 -*-
# Generated by scripts/generate_info_modules.py -- regenerate instead of editing.
# Copyright: (c) 2026, Tencent Cloud Ansible Collection Contributors
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
from __future__ import absolute_import, division, print_function

__metaclass__ = type

DOCUMENTATION = r'''
---
module: memcached_instance_info
short_description: Gather information about Tencent Cloud MEMCACHED instances
version_added: "0.8.0"
description: Returns MEMCACHED instances visible in a Tencent Cloud region.
options:
  instance_ids:
    description: Instance IDs to return.
    type: list
    elements: str
  page_size:
    description: Number of results requested per API call.
    type: int
    default: 100
extends_documentation_fragment: susunola.tencentcloud.tencentcloud
author: Tencent Cloud Ansible Collection Contributors (@susunola)
'''

EXAMPLES = r'''
- name: List all instances
  susunola.tencentcloud.memcached_instance_info:
    region: ap-guangzhou

- name: Find instances by ID
  susunola.tencentcloud.memcached_instance_info:
    region: ap-guangzhou
    instance_ids: [x-xxxxxxxx]
'''

RETURN = r'''
instances:
  description: Matching MEMCACHED instances.
  returned: always
  type: list
  elements: dict
total_count:
  description: Number of instances reported by the API.
  returned: always
  type: int
'''

from ansible.module_utils.basic import AnsibleModule
from ansible_collections.susunola.tencentcloud.plugins.module_utils.paging import Paginator
from ansible_collections.susunola.tencentcloud.plugins.module_utils.tencentcloud import (
    create_client_profile, create_credential, sdk_call, serialize_sdk_object,
    tencentcloud_argument_spec,
)


def build_request(models, instance_ids, offset, limit):
    request = models.DescribeInstancesRequest()
    request.Offset = offset
    request.Limit = limit
    if instance_ids:
        request.InstanceIds = instance_ids
    return request


def run_module():
    argument_spec = tencentcloud_argument_spec()
    argument_spec.update({
        "instance_ids": {"type": "list", "elements": "str"},
        "page_size": {"type": "int", "default": 100},
    })
    module = AnsibleModule(
        argument_spec=argument_spec,
        supports_check_mode=True,
    )
    try:
        from tencentcloud.memcached.v20190318 import models, memcached_client
    except ImportError:
        module.fail_json(msg="The tencentcloud-sdk-python-memcached package is required.")

    client = memcached_client.MemcachedClient(
        create_credential(module), module.params["region"],
        create_client_profile(module, "memcached.tencentcloudapi.com"),
    )
    paginator = Paginator(
        module.params["page_size"],
        lambda offset, limit: build_request(models, module.params["instance_ids"], offset, limit),
        lambda request: sdk_call(module, client.DescribeInstances, request),
        lambda response: response.InstanceList,
        lambda response: response.TotalNum,
    )
    item_set, total_count = paginator.fetch_all()
    instances = [serialize_sdk_object(item) for item in item_set]
    module.exit_json(changed=False, instances=instances, total_count=total_count)


def main():
    run_module()


if __name__ == "__main__":
    main()
