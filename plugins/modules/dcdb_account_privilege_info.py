#!/usr/bin/python
# -*- coding: utf-8 -*-
# Generated by scripts/generate_info_modules.py -- regenerate instead of editing.
# Copyright: (c) 2026, Tencent Cloud Ansible Collection Contributors
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
from __future__ import absolute_import, division, print_function

__metaclass__ = type

DOCUMENTATION = r'''
---
module: dcdb_account_privilege_info
short_description: Gather information about Tencent Cloud DCDB account privilege
version_added: "1.5.0"
description: Returns DCDB account privilege visible in a Tencent Cloud region.
options:
  instance_id:
    description: Instance id. API field C(InstanceId).
    type: str
  user_name:
    description: User name. API field C(UserName).
    type: str
  host:
    description: Host. API field C(Host).
    type: str
  db_name:
    description: Db name. API field C(DbName).
    type: str
  type:
    description: Type. API field C(Type).
    type: str
  object:
    description: Object. API field C(Object).
    type: str
  col_name:
    description: Col name. API field C(ColName).
    type: str
extends_documentation_fragment:
  - susunola.tencentcloud.credentials
  - susunola.tencentcloud.region
  - susunola.tencentcloud.connection
  - susunola.tencentcloud.timeout
attributes:
  check_mode:
    description:
      - Can run in C(check_mode), reading the current state and predicting
        the result without issuing a write API call.
    support: full
  idempotent:
    description:
      - Read-only, so every run returns the current state and never changes
        the target, and a repeated run reports C(changed=false).
    support: full
author: Tencent Cloud Ansible Collection Contributors (@susunola)
'''

EXAMPLES = r'''
- name: Show the account privilege
  susunola.tencentcloud.dcdb_account_privilege_info:
    region: ap-guangzhou
'''

RETURN = r'''
account_privilege:
  description: Matching DCDB account privilege.
  returned: always
  type: dict
request_id:
  description: Request ID of the API call, for cross-referencing cloud audit logs.
  returned: always
  type: str
'''

from ansible.module_utils.basic import AnsibleModule
from ansible_collections.susunola.tencentcloud.plugins.module_utils.tencentcloud import (
    create_client_profile, create_credential, sdk_call, serialize_sdk_object,
    tencentcloud_argument_spec,
)


def build_request(models, instance_id, user_name, host, db_name, type, object, col_name, offset, limit):
    # DescribeAccountPrivileges takes no request arguments and is not
    # paginated; offset and limit are accepted for signature uniformity.
    request = models.DescribeAccountPrivilegesRequest()
    if instance_id is not None:
        request.InstanceId = instance_id
    if user_name is not None:
        request.UserName = user_name
    if host is not None:
        request.Host = host
    if db_name is not None:
        request.DbName = db_name
    if type is not None:
        request.Type = type
    if object is not None:
        request.Object = object
    if col_name is not None:
        request.ColName = col_name
    return request


def run_module():
    argument_spec = tencentcloud_argument_spec()
    argument_spec.update({
        "instance_id": {"type": "str"},
        "user_name": {"type": "str"},
        "host": {"type": "str"},
        "db_name": {"type": "str"},
        "type": {"type": "str"},
        "object": {"type": "str"},
        "col_name": {"type": "str"},
    })
    module = AnsibleModule(
        argument_spec=argument_spec,
        supports_check_mode=True,
    )
    try:
        from tencentcloud.dcdb.v20180411 import models, dcdb_client
    except ImportError:
        module.fail_json(msg="The tencentcloud-sdk-python-dcdb package is required.")

    client = dcdb_client.DcdbClient(
        create_credential(module), module.params["region"],
        create_client_profile(module, "dcdb.tencentcloudapi.com"),
    )
    request = build_request(
        models,
        module.params["instance_id"],
        module.params["user_name"],
        module.params["host"],
        module.params["db_name"],
        module.params["type"],
        module.params["object"],
        module.params["col_name"],
        0,
        0)
    response = sdk_call(module, client.DescribeAccountPrivileges, request)
    account_privilege = serialize_sdk_object(response)
    account_privilege.pop("RequestId", None)
    module.exit_json(changed=False, account_privilege=account_privilege,
                     request_id=response.RequestId)


def main():
    run_module()


if __name__ == "__main__":
    main()
