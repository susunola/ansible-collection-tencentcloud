#!/usr/bin/python
# -*- coding: utf-8 -*-
# Generated by scripts/generate_info_modules.py -- regenerate instead of editing.
# Copyright: (c) 2026, Tencent Cloud Ansible Collection Contributors
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
from __future__ import absolute_import, division, print_function

__metaclass__ = type

DOCUMENTATION = r'''
---
module: dcdb_security_config_info
short_description: Gather information about Tencent Cloud DCDB db security groups
version_added: "1.5.0"
description: Returns DCDB db security groups visible in a Tencent Cloud region.
options:
  product:
    description: Sets the C(Product) field of V(DescribeDBSecurityGroups); omit it to leave that field unset.
    type: str
  instance_id:
    description: Sets the C(InstanceId) field of V(DescribeDBSecurityGroups); omit it to leave that field unset.
    type: str
extends_documentation_fragment:
  - susunola.tencentcloud.credentials
  - susunola.tencentcloud.region
  - susunola.tencentcloud.connection
  - susunola.tencentcloud.timeout
attributes:
  check_mode:
    description:
      - Can run in C(check_mode), reading the current state and predicting
        the result without issuing a write API call.
    support: full
  idempotent:
    description:
      - Read-only, so every run returns the current state and never changes
        the target, and a repeated run reports C(changed=false).
    support: full
seealso:
  - module: susunola.tencentcloud.dcdb_security_config
    description: Manage Tencent Cloud DCDB encryption, SSL and security groups.
author: Tencent Cloud Ansible Collection Contributors (@susunola)
'''

EXAMPLES = r'''
- name: List all db security groups
  susunola.tencentcloud.dcdb_security_config_info:
    region: ap-guangzhou
'''

RETURN = r'''
db_security_groups:
  description: Matching DCDB db security groups.
  returned: always
  type: list
  elements: dict
total_count:
  description: Number of db security groups returned (the API reports no total count).
  returned: always
  type: int
request_id:
  description: Request ID of the last API call, for cross-referencing cloud audit logs.
  returned: always
  type: str
'''

from ansible.module_utils.basic import AnsibleModule
from ansible_collections.susunola.tencentcloud.plugins.module_utils.tencentcloud import (
    create_client_profile, create_credential, sdk_call, serialize_sdk_object,
    tencentcloud_argument_spec,
)


def build_request(models, product, instance_id, offset, limit):
    # DescribeDBSecurityGroups returns the full list in one call and is not
    # paginated; offset and limit are accepted for signature uniformity.
    request = models.DescribeDBSecurityGroupsRequest()
    if product is not None:
        request.Product = product
    if instance_id is not None:
        request.InstanceId = instance_id
    return request


def run_module():
    argument_spec = tencentcloud_argument_spec()
    argument_spec.update({
        "product": {"type": "str"},
        "instance_id": {"type": "str"},
    })
    module = AnsibleModule(
        argument_spec=argument_spec,
        supports_check_mode=True,
    )
    try:
        from tencentcloud.dcdb.v20180411 import models, dcdb_client
    except ImportError:
        module.fail_json(msg="The tencentcloud-sdk-python-dcdb package is required.")

    client = dcdb_client.DcdbClient(
        create_credential(module), module.params["region"],
        create_client_profile(module, "dcdb.tencentcloudapi.com"),
    )
    request = build_request(models, module.params["product"], module.params["instance_id"], 0, 0)
    response = sdk_call(module, client.DescribeDBSecurityGroups, request)
    items = response.Groups or []
    db_security_groups = [serialize_sdk_object(item) for item in items]
    module.exit_json(changed=False, db_security_groups=db_security_groups,
                     total_count=len(db_security_groups), request_id=response.RequestId)


def main():
    run_module()


if __name__ == "__main__":
    main()
