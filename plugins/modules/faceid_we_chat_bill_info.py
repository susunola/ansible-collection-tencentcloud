#!/usr/bin/python
# -*- coding: utf-8 -*-
# Generated by scripts/generate_info_modules.py -- regenerate instead of editing.
# Copyright: (c) 2026, Tencent Cloud Ansible Collection Contributors
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
from __future__ import absolute_import, division, print_function

__metaclass__ = type

DOCUMENTATION = r'''
---
module: faceid_we_chat_bill_info
short_description: Gather information about Tencent Cloud FACEID we chat bills
version_added: "0.9.0"
description: Returns FACEID we chat bills visible in a Tencent Cloud region.
options: {}
extends_documentation_fragment: susunola.tencentcloud.tencentcloud
author: Tencent Cloud Ansible Collection Contributors (@susunola)
'''

EXAMPLES = r'''
- name: List all we chat bills
  susunola.tencentcloud.faceid_we_chat_bill_info:
    region: ap-guangzhou
'''

RETURN = r'''
we_chat_bills:
  description: Matching FACEID we chat bills.
  returned: always
  type: list
  elements: dict
total_count:
  description: Number of we chat bills returned (the API reports no total count).
  returned: always
  type: int
request_id:
  description: Request ID of the last API call, for cross-referencing cloud audit logs.
  returned: always
  type: str
'''

from ansible.module_utils.basic import AnsibleModule
from ansible_collections.susunola.tencentcloud.plugins.module_utils.tencentcloud import (
    create_client_profile, create_credential, sdk_call, serialize_sdk_object,
    tencentcloud_argument_spec,
)


def build_request(models, next_token, max_results):
    request = models.GetWeChatBillDetailsRequest()
    if next_token:
        request.Cursor = next_token
    return request


def run_module():
    argument_spec = tencentcloud_argument_spec()
    argument_spec.update({})
    module = AnsibleModule(
        argument_spec=argument_spec,
        supports_check_mode=True,
    )
    try:
        from tencentcloud.faceid.v20180301 import models, faceid_client
    except ImportError:
        module.fail_json(msg="The tencentcloud-sdk-python-faceid package is required.")

    client = faceid_client.FaceidClient(
        create_credential(module), module.params["region"],
        create_client_profile(module, "faceid.tencentcloudapi.com"),
    )
    we_chat_bills = []
    next_token = None
    total_count = None
    while True:
        request = build_request(
            models,
            next_token,
            None)
        response = sdk_call(module, client.GetWeChatBillDetails, request)
        batch = response.WeChatBillDetails or []
        we_chat_bills.extend(serialize_sdk_object(item) for item in batch)
        next_token = response.NextCursor
        if not response.HasNextPage or not next_token:
            break
    if total_count is None:
        total_count = len(we_chat_bills)
    request_id = getattr(response, "RequestId", None)
    module.exit_json(changed=False, we_chat_bills=we_chat_bills,
                     total_count=total_count, request_id=request_id)


def main():
    run_module()


if __name__ == "__main__":
    main()
