#!/usr/bin/python
# -*- coding: utf-8 -*-
# Generated by scripts/generate_info_modules.py -- regenerate instead of editing.
# Copyright: (c) 2026, Tencent Cloud Ansible Collection Contributors
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
from __future__ import absolute_import, division, print_function

__metaclass__ = type

DOCUMENTATION = r'''
---
module: dts_migration_job_info
short_description: Gather information about Tencent Cloud DTS migration jobs
version_added: "1.5.0"
description: Returns DTS migration jobs visible in a Tencent Cloud region.
options:
  job_id:
    description: Sets the C(JobId) field of V(DescribeMigrationJobs); omit it to leave that field unset.
    type: str
  job_name:
    description: Sets the C(JobName) field of V(DescribeMigrationJobs); omit it to leave that field unset.
    type: str
  status:
    description: Sets the C(Status) field of V(DescribeMigrationJobs); omit it to leave that field unset.
    type: list
    elements: str
  src_instance_id:
    description: Sets the C(SrcInstanceId) field of V(DescribeMigrationJobs); omit it to leave that field unset.
    type: str
  src_region:
    description: Sets the C(SrcRegion) field of V(DescribeMigrationJobs); omit it to leave that field unset.
    type: str
  src_database_type:
    description: Sets the C(SrcDatabaseType) field of V(DescribeMigrationJobs); omit it to leave that field unset.
    type: list
    elements: str
  src_access_type:
    description: Sets the C(SrcAccessType) field of V(DescribeMigrationJobs); omit it to leave that field unset.
    type: list
    elements: str
  dst_instance_id:
    description: Sets the C(DstInstanceId) field of V(DescribeMigrationJobs); omit it to leave that field unset.
    type: str
  dst_region:
    description: Sets the C(DstRegion) field of V(DescribeMigrationJobs); omit it to leave that field unset.
    type: str
  dst_database_type:
    description: Sets the C(DstDatabaseType) field of V(DescribeMigrationJobs); omit it to leave that field unset.
    type: list
    elements: str
  dst_access_type:
    description: Sets the C(DstAccessType) field of V(DescribeMigrationJobs); omit it to leave that field unset.
    type: list
    elements: str
  run_mode:
    description: Sets the C(RunMode) field of V(DescribeMigrationJobs); omit it to leave that field unset.
    type: str
  order_seq:
    description: Sets the C(OrderSeq) field of V(DescribeMigrationJobs); omit it to leave that field unset.
    type: str
  page_size:
    description: Number of results requested per API call.
    type: int
    default: 100
extends_documentation_fragment:
  - susunola.tencentcloud.credentials
  - susunola.tencentcloud.region
  - susunola.tencentcloud.connection
  - susunola.tencentcloud.timeout
attributes:
  check_mode:
    description:
      - Can run in C(check_mode), reading the current state and predicting
        the result without issuing a write API call.
    support: full
  idempotent:
    description:
      - Read-only, so every run returns the current state and never changes
        the target, and a repeated run reports C(changed=false).
    support: full
seealso:
  - module: susunola.tencentcloud.dts_migration_job
    description: Manage Tencent Cloud DTS migration jobs.
author: Tencent Cloud Ansible Collection Contributors (@susunola)
'''

EXAMPLES = r'''
- name: List all migration jobs
  susunola.tencentcloud.dts_migration_job_info:
    region: ap-guangzhou
'''

RETURN = r'''
migration_jobs:
  description: Matching DTS migration jobs.
  returned: always
  type: list
  elements: dict
total_count:
  description: Number of migration jobs reported by the API.
  returned: always
  type: int
request_id:
  description: Request ID of the last API call, for cross-referencing cloud audit logs.
  returned: always
  type: str
'''

from ansible.module_utils.basic import AnsibleModule
from ansible_collections.susunola.tencentcloud.plugins.module_utils.paging import Paginator
from ansible_collections.susunola.tencentcloud.plugins.module_utils.tencentcloud import (
    create_client_profile, create_credential, sdk_call, serialize_sdk_object,
    tencentcloud_argument_spec,
)


def build_request(models,
                  job_id,
                  job_name,
                  status,
                  src_instance_id,
                  src_region,
                  src_database_type,
                  src_access_type,
                  dst_instance_id,
                  dst_region,
                  dst_database_type,
                  dst_access_type,
                  run_mode,
                  order_seq,
                  offset,
                  limit):
    request = models.DescribeMigrationJobsRequest()
    request.Offset = offset
    request.Limit = limit
    if job_id is not None:
        request.JobId = job_id
    if job_name is not None:
        request.JobName = job_name
    if status is not None:
        request.Status = status
    if src_instance_id is not None:
        request.SrcInstanceId = src_instance_id
    if src_region is not None:
        request.SrcRegion = src_region
    if src_database_type is not None:
        request.SrcDatabaseType = src_database_type
    if src_access_type is not None:
        request.SrcAccessType = src_access_type
    if dst_instance_id is not None:
        request.DstInstanceId = dst_instance_id
    if dst_region is not None:
        request.DstRegion = dst_region
    if dst_database_type is not None:
        request.DstDatabaseType = dst_database_type
    if dst_access_type is not None:
        request.DstAccessType = dst_access_type
    if run_mode is not None:
        request.RunMode = run_mode
    if order_seq is not None:
        request.OrderSeq = order_seq
    return request


def run_module():
    argument_spec = tencentcloud_argument_spec()
    argument_spec.update({
        "job_id": {"type": "str"},
        "job_name": {"type": "str"},
        "status": {"type": "list", "elements": "str"},
        "src_instance_id": {"type": "str"},
        "src_region": {"type": "str"},
        "src_database_type": {"type": "list", "elements": "str"},
        "src_access_type": {"type": "list", "elements": "str"},
        "dst_instance_id": {"type": "str"},
        "dst_region": {"type": "str"},
        "dst_database_type": {"type": "list", "elements": "str"},
        "dst_access_type": {"type": "list", "elements": "str"},
        "run_mode": {"type": "str"},
        "order_seq": {"type": "str"},
        "page_size": {"type": "int", "default": 100},
    })
    module = AnsibleModule(
        argument_spec=argument_spec,
        supports_check_mode=True,
    )
    try:
        from tencentcloud.dts.v20211206 import models, dts_client
    except ImportError:
        module.fail_json(msg="The tencentcloud-sdk-python-dts package is required.")

    client = dts_client.DtsClient(
        create_credential(module), module.params["region"],
        create_client_profile(module, "dts.tencentcloudapi.com"),
    )
    paginator = Paginator(
        module.params["page_size"],
        lambda offset, limit: build_request(
            models,
            module.params["job_id"],
            module.params["job_name"],
            module.params["status"],
            module.params["src_instance_id"],
            module.params["src_region"],
            module.params["src_database_type"],
            module.params["src_access_type"],
            module.params["dst_instance_id"],
            module.params["dst_region"],
            module.params["dst_database_type"],
            module.params["dst_access_type"],
            module.params["run_mode"],
            module.params["order_seq"],
            offset,
            limit),
        lambda request: sdk_call(module, client.DescribeMigrationJobs, request),
        lambda response: response.JobList,
        lambda response: response.TotalCount,
    )
    item_set, total_count = paginator.fetch_all()
    migration_jobs = [serialize_sdk_object(item) for item in item_set]
    module.exit_json(changed=False, migration_jobs=migration_jobs,
                     total_count=total_count, request_id=paginator.request_id)


def main():
    run_module()


if __name__ == "__main__":
    main()
