#!/usr/bin/python
# -*- coding: utf-8 -*-
# Generated by scripts/generate_info_modules.py -- regenerate instead of editing.
# Copyright: (c) 2026, Tencent Cloud Ansible Collection Contributors
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
from __future__ import absolute_import, division, print_function

__metaclass__ = type

DOCUMENTATION = r'''
---
module: chdfs_access_rules_info
short_description: Gather information about Tencent Cloud CHDFS access rules
version_added: "1.5.0"
description: Returns CHDFS access rules visible in a Tencent Cloud region.
options:
  access_group_id:
    description: Access group id. API field C(AccessGroupId).
    type: str
extends_documentation_fragment:
  - susunola.tencentcloud.credentials
  - susunola.tencentcloud.region
  - susunola.tencentcloud.connection
  - susunola.tencentcloud.timeout
attributes:
  check_mode:
    description:
      - Can run in C(check_mode), reading the current state and predicting
        the result without issuing a write API call.
    support: full
  idempotent:
    description:
      - Read-only, so every run returns the current state and never changes
        the target, and a repeated run reports C(changed=false).
    support: full
seealso:
  - module: susunola.tencentcloud.chdfs_access_rules
    description: Reconcile Tencent Cloud CHDFS access rules.
author: Tencent Cloud Ansible Collection Contributors (@susunola)
'''

EXAMPLES = r'''
- name: List all access rules
  susunola.tencentcloud.chdfs_access_rules_info:
    region: ap-guangzhou
'''

RETURN = r'''
access_rules:
  description: Matching CHDFS access rules.
  returned: always
  type: list
  elements: dict
total_count:
  description: Number of access rules returned (the API reports no total count).
  returned: always
  type: int
request_id:
  description: Request ID of the last API call, for cross-referencing cloud audit logs.
  returned: always
  type: str
'''

from ansible.module_utils.basic import AnsibleModule
from ansible_collections.susunola.tencentcloud.plugins.module_utils.tencentcloud import (
    create_client_profile, create_credential, sdk_call, serialize_sdk_object,
    tencentcloud_argument_spec,
)


def build_request(models, access_group_id, offset, limit):
    # DescribeAccessRules returns the full list in one call and is not
    # paginated; offset and limit are accepted for signature uniformity.
    request = models.DescribeAccessRulesRequest()
    if access_group_id is not None:
        request.AccessGroupId = access_group_id
    return request


def run_module():
    argument_spec = tencentcloud_argument_spec()
    argument_spec.update({
        "access_group_id": {"type": "str"},
    })
    module = AnsibleModule(
        argument_spec=argument_spec,
        supports_check_mode=True,
    )
    try:
        from tencentcloud.chdfs.v20201112 import models, chdfs_client
    except ImportError:
        module.fail_json(msg="The tencentcloud-sdk-python-chdfs package is required.")

    client = chdfs_client.ChdfsClient(
        create_credential(module), module.params["region"],
        create_client_profile(module, "chdfs.tencentcloudapi.com"),
    )
    request = build_request(models, module.params["access_group_id"], 0, 0)
    response = sdk_call(module, client.DescribeAccessRules, request)
    items = response.AccessRules or []
    access_rules = [serialize_sdk_object(item) for item in items]
    module.exit_json(changed=False, access_rules=access_rules,
                     total_count=len(access_rules), request_id=response.RequestId)


def main():
    run_module()


if __name__ == "__main__":
    main()
