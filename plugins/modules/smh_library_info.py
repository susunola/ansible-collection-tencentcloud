#!/usr/bin/python
# -*- coding: utf-8 -*-
# Generated by scripts/generate_info_modules.py -- regenerate instead of editing.
# Copyright: (c) 2026, Tencent Cloud Ansible Collection Contributors
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
from __future__ import absolute_import, division, print_function

__metaclass__ = type

DOCUMENTATION = r'''
---
module: smh_library_info
short_description: Gather information about Tencent Cloud SMH libraries
version_added: "0.8.0"
description: Returns SMH libraries visible in a Tencent Cloud region.
options:
  library_ids:
    description: Library IDs to return.
    type: list
    elements: str
  page_size:
    description: Number of results requested per API call.
    type: int
    default: 100
extends_documentation_fragment: tencentcloud.cloud.tencentcloud
author: Tencent Cloud Ansible Collection Contributors (@susunola)
'''

EXAMPLES = r'''
- name: List all libraries
  tencentcloud.cloud.smh_library_info:
    region: ap-guangzhou

- name: Find libraries by ID
  tencentcloud.cloud.smh_library_info:
    region: ap-guangzhou
    library_ids: [x-xxxxxxxx]
'''

RETURN = r'''
libraries:
  description: Matching SMH libraries.
  returned: always
  type: list
  elements: dict
total_count:
  description: Number of libraries reported by the API.
  returned: always
  type: int
'''

from ansible.module_utils.basic import AnsibleModule
from ansible_collections.tencentcloud.cloud.plugins.module_utils.paging import Paginator
from ansible_collections.tencentcloud.cloud.plugins.module_utils.tencentcloud import (
    create_client_profile, create_credential, sdk_call, serialize_sdk_object,
    tencentcloud_argument_spec,
)


def build_request(models, library_ids, offset, limit):
    request = models.DescribeLibrariesRequest()
    request.Offset = offset
    request.Limit = limit
    if library_ids:
        request.LibraryIds = library_ids
    return request


def run_module():
    argument_spec = tencentcloud_argument_spec()
    argument_spec.update({
        "library_ids": {"type": "list", "elements": "str"},
        "page_size": {"type": "int", "default": 100},
    })
    module = AnsibleModule(
        argument_spec=argument_spec,
        supports_check_mode=True,
    )
    try:
        from tencentcloud.smh.v20210712 import models, smh_client
    except ImportError:
        module.fail_json(msg="The tencentcloud-sdk-python-smh package is required.")

    client = smh_client.SmhClient(
        create_credential(module), module.params["region"],
        create_client_profile(module, "smh.tencentcloudapi.com"),
    )
    paginator = Paginator(
        module.params["page_size"],
        lambda offset, limit: build_request(models, module.params["library_ids"], offset, limit),
        lambda request: sdk_call(module, client.DescribeLibraries, request),
        lambda response: response.List,
        lambda response: response.TotalCount,
    )
    item_set, total_count = paginator.fetch_all()
    libraries = [serialize_sdk_object(item) for item in item_set]
    module.exit_json(changed=False, libraries=libraries, total_count=total_count)


def main():
    run_module()


if __name__ == "__main__":
    main()
