#!/usr/bin/python
# -*- coding: utf-8 -*-
# Generated by scripts/generate_info_modules.py -- regenerate instead of editing.
# Copyright: (c) 2026, Tencent Cloud Ansible Collection Contributors
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
from __future__ import absolute_import, division, print_function

__metaclass__ = type

DOCUMENTATION = r'''
---
module: cfw_address_template_info
short_description: Gather information about Tencent Cloud CFW address templates
version_added: "1.5.0"
description: Returns CFW address templates visible in a Tencent Cloud region.
options:
  by:
    description: By. API field C(By).
    type: str
  order:
    description: Order. API field C(Order).
    type: str
  search_value:
    description: Search value. API field C(SearchValue).
    type: str
  uuid:
    description: Uuid. API field C(Uuid).
    type: str
  template_type:
    description: Template type. API field C(TemplateType).
    type: str
  template_id:
    description: Template id. API field C(TemplateId).
    type: str
  page_size:
    description: Number of results requested per API call.
    type: int
    default: 100
extends_documentation_fragment:
  - susunola.tencentcloud.credentials
  - susunola.tencentcloud.region
  - susunola.tencentcloud.connection
  - susunola.tencentcloud.timeout
author: Tencent Cloud Ansible Collection Contributors (@susunola)
'''

EXAMPLES = r'''
- name: List all address templates
  susunola.tencentcloud.cfw_address_template_info:
    region: ap-guangzhou
'''

RETURN = r'''
address_templates:
  description: Matching CFW address templates.
  returned: always
  type: list
  elements: dict
total_count:
  description: Number of address templates reported by the API.
  returned: always
  type: int
request_id:
  description: Request ID of the last API call, for cross-referencing cloud audit logs.
  returned: always
  type: str
'''

from ansible.module_utils.basic import AnsibleModule
from ansible_collections.susunola.tencentcloud.plugins.module_utils.paging import Paginator
from ansible_collections.susunola.tencentcloud.plugins.module_utils.tencentcloud import (
    create_client_profile, create_credential, sdk_call, serialize_sdk_object,
    tencentcloud_argument_spec,
)


def build_request(models, by, order, search_value, uuid, template_type, template_id, offset, limit):
    request = models.DescribeAddressTemplateListRequest()
    request.Offset = offset
    request.Limit = limit
    if by is not None:
        request.By = by
    if order is not None:
        request.Order = order
    if search_value is not None:
        request.SearchValue = search_value
    if uuid is not None:
        request.Uuid = uuid
    if template_type is not None:
        request.TemplateType = template_type
    if template_id is not None:
        request.TemplateId = template_id
    return request


def run_module():
    argument_spec = tencentcloud_argument_spec()
    argument_spec.update({
        "by": {"type": "str"},
        "order": {"type": "str"},
        "search_value": {"type": "str"},
        "uuid": {"type": "str"},
        "template_type": {"type": "str"},
        "template_id": {"type": "str"},
        "page_size": {"type": "int", "default": 100},
    })
    module = AnsibleModule(
        argument_spec=argument_spec,
        supports_check_mode=True,
    )
    try:
        from tencentcloud.cfw.v20190904 import models, cfw_client
    except ImportError:
        module.fail_json(msg="The tencentcloud-sdk-python-cfw package is required.")

    client = cfw_client.CfwClient(
        create_credential(module), module.params["region"],
        create_client_profile(module, "cfw.tencentcloudapi.com"),
    )
    paginator = Paginator(
        module.params["page_size"],
        lambda offset, limit: build_request(
            models,
            module.params["by"],
            module.params["order"],
            module.params["search_value"],
            module.params["uuid"],
            module.params["template_type"],
            module.params["template_id"],
            offset,
            limit),
        lambda request: sdk_call(module, client.DescribeAddressTemplateList, request),
        lambda response: response.Data,
        lambda response: response.Total,
    )
    item_set, total_count = paginator.fetch_all()
    address_templates = [serialize_sdk_object(item) for item in item_set]
    module.exit_json(changed=False, address_templates=address_templates,
                     total_count=total_count, request_id=paginator.request_id)


def main():
    run_module()


if __name__ == "__main__":
    main()
