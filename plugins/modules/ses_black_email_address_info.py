#!/usr/bin/python
# -*- coding: utf-8 -*-
# Generated by scripts/generate_info_modules.py -- regenerate instead of editing.
# Copyright: (c) 2026, Tencent Cloud Ansible Collection Contributors
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
from __future__ import absolute_import, division, print_function

__metaclass__ = type

DOCUMENTATION = r'''
---
module: ses_black_email_address_info
short_description: Gather information about Tencent Cloud SES black email addresses
version_added: "0.8.0"
description: Returns SES black email addresses visible in a Tencent Cloud region.
options:
  start_date:
    description: Start date in YYYY-MM-DD format (required by the API).
    type: str
    required: true
  end_date:
    description: End date in YYYY-MM-DD format (required by the API).
    type: str
    required: true
  page_size:
    description: Number of results requested per API call.
    type: int
    default: 100
extends_documentation_fragment: susunola.tencentcloud.tencentcloud
author: Tencent Cloud Ansible Collection Contributors (@susunola)
'''

EXAMPLES = r'''
- name: List black email addresses in a date range
  susunola.tencentcloud.ses_black_email_address_info:
    region: ap-singapore
    start_date: "2026-01-01"
    end_date: "2026-01-31"
'''

RETURN = r'''
black_email_addresses:
  description: Matching SES black email addresses.
  returned: always
  type: list
  elements: dict
total_count:
  description: Number of black email addresses reported by the API.
  returned: always
  type: int
request_id:
  description: Request ID of the last API call, for cross-referencing cloud audit logs.
  returned: always
  type: str
'''

from ansible.module_utils.basic import AnsibleModule
from ansible_collections.susunola.tencentcloud.plugins.module_utils.paging import Paginator
from ansible_collections.susunola.tencentcloud.plugins.module_utils.tencentcloud import (
    create_client_profile, create_credential, sdk_call, serialize_sdk_object,
    tencentcloud_argument_spec,
)


def build_request(models, start_date, end_date, offset, limit):
    request = models.ListBlackEmailAddressRequest()
    request.Offset = offset
    request.Limit = limit
    request.StartDate = start_date
    request.EndDate = end_date
    return request


def run_module():
    argument_spec = tencentcloud_argument_spec()
    argument_spec.update({
        "start_date": {"type": "str", "required": True},
        "end_date": {"type": "str", "required": True},
        "page_size": {"type": "int", "default": 100},
    })
    module = AnsibleModule(
        argument_spec=argument_spec,
        supports_check_mode=True,
    )
    try:
        from tencentcloud.ses.v20201002 import models, ses_client
    except ImportError:
        module.fail_json(msg="The tencentcloud-sdk-python-ses package is required.")

    client = ses_client.SesClient(
        create_credential(module), module.params["region"],
        create_client_profile(module, "ses.tencentcloudapi.com"),
    )
    paginator = Paginator(
        module.params["page_size"],
        lambda offset, limit: build_request(
            models,
            module.params["start_date"],
            module.params["end_date"],
            offset,
            limit),
        lambda request: sdk_call(module, client.ListBlackEmailAddress, request),
        lambda response: response.BlackList,
        lambda response: response.TotalCount,
    )
    item_set, total_count = paginator.fetch_all()
    black_email_addresses = [serialize_sdk_object(item) for item in item_set]
    module.exit_json(changed=False, black_email_addresses=black_email_addresses,
                     total_count=total_count, request_id=paginator.request_id)


def main():
    run_module()


if __name__ == "__main__":
    main()
