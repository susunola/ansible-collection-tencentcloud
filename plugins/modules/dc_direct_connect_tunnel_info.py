#!/usr/bin/python
# -*- coding: utf-8 -*-
# Generated by scripts/generate_info_modules.py -- regenerate instead of editing.
# Copyright: (c) 2026, Tencent Cloud Ansible Collection Contributors
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
from __future__ import absolute_import, division, print_function

__metaclass__ = type

DOCUMENTATION = r'''
---
module: dc_direct_connect_tunnel_info
short_description: Gather information about Tencent Cloud DC direct connect tunnels
version_added: "0.8.0"
description: Returns DC direct connect tunnels visible in a Tencent Cloud region.
options:
  direct_connect_tunnel_ids:
    description: Direct connect tunnel IDs to return. Mutually exclusive with O(filters).
    type: list
    elements: str
  filters:
    description: DC API filter names mapped to lists of values.
    type: dict
    default: {}
  page_size:
    description: Number of results requested per API call.
    type: int
    default: 100
extends_documentation_fragment: tencentcloud.cloud.tencentcloud
author: Tencent Cloud Ansible Collection Contributors (@susunola)
'''

EXAMPLES = r'''
- name: List all direct connect tunnels
  tencentcloud.cloud.dc_direct_connect_tunnel_info:
    region: ap-guangzhou

- name: Find direct connect tunnels by ID
  tencentcloud.cloud.dc_direct_connect_tunnel_info:
    region: ap-guangzhou
    direct_connect_tunnel_ids: [x-xxxxxxxx]
'''

RETURN = r'''
direct_connect_tunnels:
  description: Matching DC direct connect tunnels.
  returned: always
  type: list
  elements: dict
total_count:
  description: Number of direct connect tunnels reported by the API.
  returned: always
  type: int
'''

from ansible.module_utils.basic import AnsibleModule
from ansible_collections.tencentcloud.cloud.plugins.module_utils.paging import Paginator
from ansible_collections.tencentcloud.cloud.plugins.module_utils.tencentcloud import (
    create_client_profile, create_credential, sdk_call, serialize_sdk_object,
    tencentcloud_argument_spec,
)


def build_request(models, direct_connect_tunnel_ids, filters, offset, limit):
    request = models.DescribeDirectConnectTunnelsRequest()
    request.Offset = offset
    request.Limit = limit
    if direct_connect_tunnel_ids:
        request.DirectConnectTunnelIds = direct_connect_tunnel_ids
    if filters:
        request.Filters = []
        for name, values in sorted(filters.items()):
            api_filter = models.Filter()
            api_filter.Name = name
            api_filter.Values = values if isinstance(values, list) else [values]
            request.Filters.append(api_filter)
    return request


def run_module():
    argument_spec = tencentcloud_argument_spec()
    argument_spec.update({
        "direct_connect_tunnel_ids": {"type": "list", "elements": "str"},
        "filters": {"type": "dict", "default": {}},
        "page_size": {"type": "int", "default": 100},
    })
    module = AnsibleModule(
        argument_spec=argument_spec,
        mutually_exclusive=[("direct_connect_tunnel_ids", "filters")],
        supports_check_mode=True,
    )
    try:
        from tencentcloud.dc.v20180410 import models, dc_client
    except ImportError:
        module.fail_json(msg="The tencentcloud-sdk-python-dc package is required.")

    client = dc_client.DcClient(
        create_credential(module), module.params["region"],
        create_client_profile(module, "dc.tencentcloudapi.com"),
    )
    paginator = Paginator(
        module.params["page_size"],
        lambda offset, limit: build_request(
            models,
            module.params["direct_connect_tunnel_ids"],
            module.params["filters"],
            offset,
            limit),
        lambda request: sdk_call(module, client.DescribeDirectConnectTunnels, request),
        lambda response: response.DirectConnectTunnelSet,
        lambda response: response.TotalCount,
    )
    item_set, total_count = paginator.fetch_all()
    direct_connect_tunnels = [serialize_sdk_object(item) for item in item_set]
    module.exit_json(changed=False, direct_connect_tunnels=direct_connect_tunnels, total_count=total_count)


def main():
    run_module()


if __name__ == "__main__":
    main()
