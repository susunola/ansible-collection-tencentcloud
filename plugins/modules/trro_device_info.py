#!/usr/bin/python
# -*- coding: utf-8 -*-
# Generated by scripts/generate_info_modules.py -- regenerate instead of editing.
# Copyright: (c) 2026, Tencent Cloud Ansible Collection Contributors
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
from __future__ import absolute_import, division, print_function

__metaclass__ = type

DOCUMENTATION = r'''
---
module: trro_device_info
short_description: Gather information about Tencent Cloud TRRO devices
version_added: "0.8.0"
description: Returns TRRO devices visible in a Tencent Cloud region.
options:
  project_id:
    description: Project ID the devices belong to.
    type: str
    required: true
  page_size:
    description: Number of results requested per API call.
    type: int
    default: 100
extends_documentation_fragment: susunola.tencentcloud.tencentcloud
author: Tencent Cloud Ansible Collection Contributors (@susunola)
'''

EXAMPLES = r'''
- name: List devices of a project
  susunola.tencentcloud.trro_device_info:
    region: ap-guangzhou
    project_id: f3glr49ry0i0xlm7
'''

RETURN = r'''
devices:
  description: Matching TRRO devices.
  returned: always
  type: list
  elements: dict
total_count:
  description: Number of devices reported by the API.
  returned: always
  type: int
request_id:
  description: Request ID of the last API call, for cross-referencing cloud audit logs.
  returned: always
  type: str
'''

from ansible.module_utils.basic import AnsibleModule
from ansible_collections.susunola.tencentcloud.plugins.module_utils.paging import Paginator
from ansible_collections.susunola.tencentcloud.plugins.module_utils.tencentcloud import (
    create_client_profile, create_credential, sdk_call, serialize_sdk_object,
    tencentcloud_argument_spec,
)


def build_request(models, project_id, offset, limit):
    request = models.DescribeDeviceListRequest()
    request.PageNumber = offset // limit + 1
    request.PageSize = limit
    request.ProjectId = project_id
    return request


def run_module():
    argument_spec = tencentcloud_argument_spec()
    argument_spec.update({
        "project_id": {"type": "str", "required": True},
        "page_size": {"type": "int", "default": 100},
    })
    module = AnsibleModule(
        argument_spec=argument_spec,
        supports_check_mode=True,
    )
    try:
        from tencentcloud.trro.v20220325 import models, trro_client
    except ImportError:
        module.fail_json(msg="The tencentcloud-sdk-python-trro package is required.")

    client = trro_client.TrroClient(
        create_credential(module), module.params["region"],
        create_client_profile(module, "trro.tencentcloudapi.com"),
    )
    paginator = Paginator(
        module.params["page_size"],
        lambda offset, limit: build_request(models, module.params["project_id"], offset, limit),
        lambda request: sdk_call(module, client.DescribeDeviceList, request),
        lambda response: response.Devices,
        lambda response: response.Total,
    )
    item_set, total_count = paginator.fetch_all()
    devices = [serialize_sdk_object(item) for item in item_set]
    module.exit_json(changed=False, devices=devices,
                     total_count=total_count, request_id=paginator.request_id)


def main():
    run_module()


if __name__ == "__main__":
    main()
