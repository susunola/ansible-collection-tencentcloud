#!/usr/bin/python
# -*- coding: utf-8 -*-
# Generated by scripts/generate_info_modules.py -- regenerate instead of editing.
# Copyright: (c) 2026, Tencent Cloud Ansible Collection Contributors
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
from __future__ import absolute_import, division, print_function

__metaclass__ = type

DOCUMENTATION = r'''
---
module: clb_load_balancer_info
short_description: Gather information about Tencent Cloud CLB load balancers
version_added: "0.4.0"
description: Returns CLB (Cloud Load Balancer) instances visible in a Tencent Cloud region.
options:
  load_balancer_ids:
    description: Load balancer IDs to return. Mutually exclusive with O(filters).
    type: list
    elements: str
  filters:
    description: CLB API filter names mapped to lists of values.
    type: dict
    default: {}
  page_size:
    description: Number of results requested per API call.
    type: int
    default: 100
extends_documentation_fragment: tencentcloud.cloud.tencentcloud
author: Tencent Cloud Ansible Collection Contributors (@susunola)
'''

EXAMPLES = r'''
- name: List all CLB load balancers
  tencentcloud.cloud.clb_load_balancer_info:
    region: ap-guangzhou

- name: Find load balancers by ID
  tencentcloud.cloud.clb_load_balancer_info:
    region: ap-guangzhou
    load_balancer_ids: [lb-xxxxxxxx]
'''

RETURN = r'''
load_balancers:
  description: Matching CLB load balancers.
  returned: always
  type: list
  elements: dict
total_count:
  description: Number of load balancers reported by the API.
  returned: always
  type: int
'''

from ansible.module_utils.basic import AnsibleModule
from ansible_collections.tencentcloud.cloud.plugins.module_utils.paging import Paginator
from ansible_collections.tencentcloud.cloud.plugins.module_utils.tencentcloud import (
    create_client_profile, create_credential, sdk_call, serialize_sdk_object,
    tencentcloud_argument_spec,
)


def build_request(models, load_balancer_ids, filters, offset, limit):
    request = models.DescribeLoadBalancersRequest()
    request.Offset = offset
    request.Limit = limit
    if load_balancer_ids:
        request.LoadBalancerIds = load_balancer_ids
    if filters:
        request.Filters = []
        for name, values in sorted(filters.items()):
            api_filter = models.Filter()
            api_filter.Name = name
            api_filter.Values = values if isinstance(values, list) else [values]
            request.Filters.append(api_filter)
    return request


def run_module():
    argument_spec = tencentcloud_argument_spec()
    argument_spec.update({
        "load_balancer_ids": {"type": "list", "elements": "str"},
        "filters": {"type": "dict", "default": {}},
        "page_size": {"type": "int", "default": 100},
    })
    module = AnsibleModule(
        argument_spec=argument_spec,
        mutually_exclusive=[("load_balancer_ids", "filters")],
        supports_check_mode=True,
    )
    try:
        from tencentcloud.clb.v20180317 import models, clb_client
    except ImportError:
        module.fail_json(msg="The tencentcloud-sdk-python-clb package is required.")

    client = clb_client.ClbClient(
        create_credential(module), module.params["region"],
        create_client_profile(module, "clb.tencentcloudapi.com"),
    )
    paginator = Paginator(
        module.params["page_size"],
        lambda offset, limit: build_request(
            models,
            module.params["load_balancer_ids"],
            module.params["filters"],
            offset,
            limit),
        lambda request: sdk_call(module, client.DescribeLoadBalancers, request),
        lambda response: response.LoadBalancerSet,
        lambda response: response.TotalCount,
    )
    item_set, total_count = paginator.fetch_all()
    load_balancers = [serialize_sdk_object(item) for item in item_set]
    module.exit_json(changed=False, load_balancers=load_balancers, total_count=total_count)


def main():
    run_module()


if __name__ == "__main__":
    main()
