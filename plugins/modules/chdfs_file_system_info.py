#!/usr/bin/python
# -*- coding: utf-8 -*-
# Generated by scripts/generate_info_modules.py -- regenerate instead of editing.
# Copyright: (c) 2026, Tencent Cloud Ansible Collection Contributors
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
from __future__ import absolute_import, division, print_function

__metaclass__ = type

DOCUMENTATION = r'''
---
module: chdfs_file_system_info
short_description: Gather information about Tencent Cloud CHDFS file systems
version_added: "0.9.0"
description: Returns CHDFS file systems visible in a Tencent Cloud region.
options: {}
extends_documentation_fragment: susunola.tencentcloud.tencentcloud
author: Tencent Cloud Ansible Collection Contributors (@susunola)
'''

EXAMPLES = r'''
- name: List all file systems
  susunola.tencentcloud.chdfs_file_system_info:
    region: ap-guangzhou
'''

RETURN = r'''
file_systems:
  description: Matching CHDFS file systems.
  returned: always
  type: list
  elements: dict
total_count:
  description: Number of file systems returned (the API reports no total count).
  returned: always
  type: int
request_id:
  description: Request ID of the last API call, for cross-referencing cloud audit logs.
  returned: always
  type: str
'''

from ansible.module_utils.basic import AnsibleModule
from ansible_collections.susunola.tencentcloud.plugins.module_utils.tencentcloud import (
    create_client_profile, create_credential, sdk_call, serialize_sdk_object,
    tencentcloud_argument_spec,
)


def build_request(models, next_token, max_results):
    request = models.DescribeFileSystemsRequest()
    if next_token:
        request.FileSystemIdMarker = next_token
    return request


def run_module():
    argument_spec = tencentcloud_argument_spec()
    argument_spec.update({})
    module = AnsibleModule(
        argument_spec=argument_spec,
        supports_check_mode=True,
    )
    try:
        from tencentcloud.chdfs.v20201112 import models, chdfs_client
    except ImportError:
        module.fail_json(msg="The tencentcloud-sdk-python-chdfs package is required.")

    client = chdfs_client.ChdfsClient(
        create_credential(module), module.params["region"],
        create_client_profile(module, "chdfs.tencentcloudapi.com"),
    )
    file_systems = []
    next_token = None
    total_count = None
    while True:
        request = build_request(
            models,
            next_token,
            None)
        response = sdk_call(module, client.DescribeFileSystems, request)
        batch = response.FileSystems or []
        file_systems.extend(serialize_sdk_object(item) for item in batch)
        next_token = response.NextFileSystemIdMarker
        if response.IsOver or not next_token:
            break
    if total_count is None:
        total_count = len(file_systems)
    request_id = getattr(response, "RequestId", None)
    module.exit_json(changed=False, file_systems=file_systems,
                     total_count=total_count, request_id=request_id)


def main():
    run_module()


if __name__ == "__main__":
    main()
