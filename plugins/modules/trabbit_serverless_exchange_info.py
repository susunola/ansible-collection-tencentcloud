#!/usr/bin/python
# -*- coding: utf-8 -*-
# Generated by scripts/generate_info_modules.py -- regenerate instead of editing.
# Copyright: (c) 2026, Tencent Cloud Ansible Collection Contributors
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
from __future__ import absolute_import, division, print_function

__metaclass__ = type

DOCUMENTATION = r'''
---
module: trabbit_serverless_exchange_info
short_description: Gather information about Tencent Cloud TRABBIT serverless exchanges
version_added: "1.5.0"
description: Returns TRABBIT serverless exchanges visible in a Tencent Cloud region.
options:
  instance_id:
    description: Sets the C(InstanceId) field of V(DescribeRabbitMQServerlessExchanges); omit it to leave that field unset.
    type: str
  virtual_host:
    description: Sets the C(VirtualHost) field of V(DescribeRabbitMQServerlessExchanges); omit it to leave that field unset.
    type: str
  search_word:
    description: Sets the C(SearchWord) field of V(DescribeRabbitMQServerlessExchanges); omit it to leave that field unset.
    type: str
  exchange_type_filters:
    description: Sets the C(ExchangeTypeFilters) field of V(DescribeRabbitMQServerlessExchanges); omit it to leave that field unset.
    type: list
    elements: str
  exchange_creator_filters:
    description: Sets the C(ExchangeCreatorFilters) field of V(DescribeRabbitMQServerlessExchanges); omit it to leave that field unset.
    type: list
    elements: str
  exchange_name:
    description: Sets the C(ExchangeName) field of V(DescribeRabbitMQServerlessExchanges); omit it to leave that field unset.
    type: str
  sort_element:
    description: Sets the C(SortElement) field of V(DescribeRabbitMQServerlessExchanges); omit it to leave that field unset.
    type: str
  sort_order:
    description: Sets the C(SortOrder) field of V(DescribeRabbitMQServerlessExchanges); omit it to leave that field unset.
    type: str
  page_size:
    description: Number of results requested per API call.
    type: int
    default: 100
extends_documentation_fragment:
  - susunola.tencentcloud.credentials
  - susunola.tencentcloud.region
  - susunola.tencentcloud.connection
  - susunola.tencentcloud.timeout
attributes:
  check_mode:
    description:
      - Can run in C(check_mode), reading the current state and predicting
        the result without issuing a write API call.
    support: full
  idempotent:
    description:
      - Read-only, so every run returns the current state and never changes
        the target, and a repeated run reports C(changed=false).
    support: full
seealso:
  - module: susunola.tencentcloud.trabbit_serverless_exchange
    description: Manage Tencent Cloud RabbitMQ Serverless exchanges.
author: Tencent Cloud Ansible Collection Contributors (@susunola)
'''

EXAMPLES = r'''
- name: List all serverless exchanges
  susunola.tencentcloud.trabbit_serverless_exchange_info:
    region: ap-guangzhou
'''

RETURN = r'''
serverless_exchanges:
  description: Matching TRABBIT serverless exchanges.
  returned: always
  type: list
  elements: dict
total_count:
  description: Number of serverless exchanges reported by the API.
  returned: always
  type: int
request_id:
  description: Request ID of the last API call, for cross-referencing cloud audit logs.
  returned: always
  type: str
'''

from ansible.module_utils.basic import AnsibleModule
from ansible_collections.susunola.tencentcloud.plugins.module_utils.paging import Paginator
from ansible_collections.susunola.tencentcloud.plugins.module_utils.tencentcloud import (
    create_client_profile, create_credential, sdk_call, serialize_sdk_object,
    tencentcloud_argument_spec,
)


def build_request(models,
                  instance_id,
                  virtual_host,
                  search_word,
                  exchange_type_filters,
                  exchange_creator_filters,
                  exchange_name,
                  sort_element,
                  sort_order,
                  offset,
                  limit):
    request = models.DescribeRabbitMQServerlessExchangesRequest()
    request.Offset = offset
    request.Limit = limit
    if instance_id is not None:
        request.InstanceId = instance_id
    if virtual_host is not None:
        request.VirtualHost = virtual_host
    if search_word is not None:
        request.SearchWord = search_word
    if exchange_type_filters is not None:
        request.ExchangeTypeFilters = exchange_type_filters
    if exchange_creator_filters is not None:
        request.ExchangeCreatorFilters = exchange_creator_filters
    if exchange_name is not None:
        request.ExchangeName = exchange_name
    if sort_element is not None:
        request.SortElement = sort_element
    if sort_order is not None:
        request.SortOrder = sort_order
    return request


def run_module():
    argument_spec = tencentcloud_argument_spec()
    argument_spec.update({
        "instance_id": {"type": "str"},
        "virtual_host": {"type": "str"},
        "search_word": {"type": "str"},
        "exchange_type_filters": {"type": "list", "elements": "str"},
        "exchange_creator_filters": {"type": "list", "elements": "str"},
        "exchange_name": {"type": "str"},
        "sort_element": {"type": "str"},
        "sort_order": {"type": "str"},
        "page_size": {"type": "int", "default": 100},
    })
    module = AnsibleModule(
        argument_spec=argument_spec,
        supports_check_mode=True,
    )
    try:
        from tencentcloud.trabbit.v20230418 import models, trabbit_client
    except ImportError:
        module.fail_json(msg="The tencentcloud-sdk-python-trabbit package is required.")

    client = trabbit_client.TrabbitClient(
        create_credential(module), module.params["region"],
        create_client_profile(module, "trabbit.tencentcloudapi.com"),
    )
    paginator = Paginator(
        module.params["page_size"],
        lambda offset, limit: build_request(
            models,
            module.params["instance_id"],
            module.params["virtual_host"],
            module.params["search_word"],
            module.params["exchange_type_filters"],
            module.params["exchange_creator_filters"],
            module.params["exchange_name"],
            module.params["sort_element"],
            module.params["sort_order"],
            offset,
            limit),
        lambda request: sdk_call(module, client.DescribeRabbitMQServerlessExchanges, request),
        lambda response: response.ExchangeInfoList,
        lambda response: response.TotalCount,
    )
    item_set, total_count = paginator.fetch_all()
    serverless_exchanges = [serialize_sdk_object(item) for item in item_set]
    module.exit_json(changed=False, serverless_exchanges=serverless_exchanges,
                     total_count=total_count, request_id=paginator.request_id)


def main():
    run_module()


if __name__ == "__main__":
    main()
