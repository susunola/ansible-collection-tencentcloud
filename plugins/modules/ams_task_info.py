#!/usr/bin/python
# -*- coding: utf-8 -*-
# Generated by scripts/generate_info_modules.py -- regenerate instead of editing.
# Copyright: (c) 2026, Tencent Cloud Ansible Collection Contributors
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
from __future__ import absolute_import, division, print_function

__metaclass__ = type

DOCUMENTATION = r'''
---
module: ams_task_info
short_description: Gather information about Tencent Cloud AMS tasks
version_added: "0.9.0"
description: Returns AMS tasks visible in a Tencent Cloud region.
options:
  page_size:
    description: Number of results requested per API call.
    type: int
    default: 100
extends_documentation_fragment: susunola.tencentcloud.tencentcloud
author: Tencent Cloud Ansible Collection Contributors (@susunola)
'''

EXAMPLES = r'''
- name: List all tasks
  susunola.tencentcloud.ams_task_info:
    region: ap-guangzhou
'''

RETURN = r'''
tasks:
  description: Matching AMS tasks.
  returned: always
  type: list
  elements: dict
total_count:
  description: Number of tasks returned (the API reports no total count).
  returned: always
  type: int
'''

from ansible.module_utils.basic import AnsibleModule
from ansible_collections.susunola.tencentcloud.plugins.module_utils.tencentcloud import (
    create_client_profile, create_credential, sdk_call, serialize_sdk_object,
    tencentcloud_argument_spec,
)


def build_request(models, next_token, max_results):
    request = models.DescribeTasksRequest()
    request.Limit = max_results
    if next_token:
        request.PageToken = next_token
    return request


def run_module():
    argument_spec = tencentcloud_argument_spec()
    argument_spec.update({
        "page_size": {"type": "int", "default": 100},
    })
    module = AnsibleModule(
        argument_spec=argument_spec,
        supports_check_mode=True,
    )
    try:
        from tencentcloud.ams.v20201229 import models, ams_client
    except ImportError:
        module.fail_json(msg="The tencentcloud-sdk-python-ams package is required.")

    client = ams_client.AmsClient(
        create_credential(module), module.params["region"],
        create_client_profile(module, "ams.tencentcloudapi.com"),
    )
    tasks = []
    next_token = None
    total_count = None
    while True:
        request = build_request(
            models,
            next_token,
            module.params["page_size"])
        response = sdk_call(module, client.DescribeTasks, request)
        batch = response.Data or []
        tasks.extend(serialize_sdk_object(item) for item in batch)
        next_token = response.PageToken
        if not next_token:
            break
    if total_count is None:
        total_count = len(tasks)
    module.exit_json(changed=False, tasks=tasks, total_count=total_count)


def main():
    run_module()


if __name__ == "__main__":
    main()
