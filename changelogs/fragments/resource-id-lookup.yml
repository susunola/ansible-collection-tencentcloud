minor_changes:
  - lookup resource_id - add exact-name, paginated CFS file-system resolution.
  - >-
    lookup plugins - add ``resource_id`` for exact, ambiguity-safe name-to-ID
    resolution across VPC, subnet, security group, CVM, CLB, TKE and CDB.
  - >-
    resource_id lookup - add STS AssumeRole, endpoint override, request timeout
    and SDK client-identifier options consistent with resource modules.
  - >-
    resource_id lookup - expand name resolution to Redis, MongoDB, API Gateway
    services, TCR instances, TEM environments and TEM applications.
  - >-
    resource_id lookup - paginate name searches and retain ambiguity checks
    across page boundaries for large accounts.
