minor_changes:
  - >-
    solution roles - add ``tc_postgresql_stack`` for PostgreSQL instances,
    accounts, backup plans and reusable parameter templates with exact-name
    adoption and explicit two-stage isolation and purge semantics.
