# tc_emr_platform

Provisions an EMR cluster and reconciles named load-based or time-based automatic
scaling strategies. Cluster termination is disabled unless
`tc_emr_platform_allow_destroy` is explicitly enabled.
